# =====================================================
# CONFIGURACIÓN GENERAL DEL DASHBOARD UCC
# =====================================================

cache:
  ttl_minutos: 60

rutas:
  data_procesada: "data/processed"
  data_cruda: "data/raw"
